# === Required ===
GCP_PROJECT=my-project-id                  # GCP project ID
DRIVE_FOLDER_ID=1ABC...xyz                 # Root Drive folder to monitor
GIT_REPO_URL=https://github.com/org/repo   # Any git host (GitHub, GitLab, Bitbucket, etc.)
GIT_BRANCH=main                            # Branch to push to
GIT_TOKEN_SECRET=git-token                  # Secret Manager secret name for git auth token

# === Optional ===
EXCLUDE_PATHS=Drafts/*,Archive/*,Temp/*     # Skip these subfolder patterns (glob-style, comma-separated)
SKIP_EXTENSIONS=.zip,.exe,.dmg,.iso         # Don't commit files with these extensions
MAX_FILE_SIZE_MB=100                        # Skip files larger than this (default: 100)
COMMIT_AUTHOR_NAME=Drive Sync Bot           # Fallback author when lastModifyingUser unavailable
COMMIT_AUTHOR_EMAIL=sync@yourfirm.com       # Fallback author email
FIRESTORE_COLLECTION=drive_sync_state       # Firestore root collection (default: drive_sync_state)
DOCS_SUBDIR=                                # Subdirectory in git repo (default: Drive folder name)
SYNC_TRIGGER_SECRET=                        # Shared secret for manual/scheduler channel-less sync triggers
